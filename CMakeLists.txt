cmake_minimum_required(VERSION 3.16)
project("printer-server")
if(UNIX AND NOT APPLE)
	set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wno-narrowing -pthread")
endif()
add_subdirectory("vendor")
add_subdirectory("src")
